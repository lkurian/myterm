1. Run install.sh
3. Open vim and run ":BundleInstall" to setup all the vim plugins
2. Open "Iterm2 -> Preferences -> Profile -> Text -> ChangeFont" and set the font to "Source Code Pro" under the "English" collection.
